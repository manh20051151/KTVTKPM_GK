package com.example.SubjectRevice.repositories;

import com.example.SubjectRevice.models.Subject;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SubjectRepositoty extends JpaRepository<Subject, Long> {
}
